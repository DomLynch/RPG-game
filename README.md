# Share buttons, C redo (Dom via Lead, 2026-09-25)

These redo C without the circles, in the same place: left of Next, above the joystick, over real trunk 52953b51 kill frames at 375x812.
Dark = The Night Pit (`?arena=a`), light = Blood Sand (`?arena=c`), both Nightborn kills after "Leave it". Next is drawn where trunk puts it.

- **C1**: icon + text (SHARE / CLIP), no circle.
- **C2**: text only, no circle, no icon.
- **C3**: icon only, no circle, no text.

**Opacity: 0.75** (white). For legibility on pale sand each also carries a soft dark drop shadow: 0 1px 2px at 0.85 and 0 0 6px at 0.45. There's no plate and no ring.
The tap target stays 60x60 in every variant (invisible). Only the contents are drawn.

Files: `c-sheet.png` (top row dark, bottom row light, C1 | C2 | C3), `c-crops.png` (the button region, all six), one still per variant per floor, and the two source frames.
Recording state (Lead's condition) = the Export control's own state in the same place, nothing over the fight. It's drawn after the pick.
