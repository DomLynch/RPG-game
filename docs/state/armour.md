# Armour — project state

Lane opened 2026-09-26 15:2x +04 by Strategy on Dom's order ("open a new armour lane, as we have a weapons lane"). Append new entries at the TOP. Keep evidence and remaining validation in every entry (AGENTS.md). Folder `~/Developer/frankendom-armour`, session name **Frankendom - Armour**, key `armour`.

## 2026-09-26 19:5x — Sets: re-dress path closed, generation recipe owns the look; Armour owns the SETS (Strategy ruling via Lead)

**What happened (19:07–19:55, the FREE after 36d4aecc).** Three set proposals as scratch builds on trunk (never committed; the scratch is a 138-line patch kept by the lane), each a 375 strip (Profile figure crop beside the fight-camera cell) on `origin/evidence/armour-sets`:
- Bronze Hoplite v2 (5a8dc51f, 19:18): Centurion kit re-dressed — tunic/studs/wraps in a burnished 'Bronze plate', bronze shoulder domes, crest 1.8×/1.5×/1.6× seated on the helm, shield r .36 bronze with a red ring-and-rays device. Strategy: PASSES the fight-camera outline test (plume + domes + disc: the first rank change that made a different figure from behind); with Dom as a proposal.
- Goblin Scrap-King v3 (f35285bf, 19:47): bare chest (tunic mesh dropped; the doll's Body layer re-renders the whole figure minus the tunic), two bone horns on the scrap helm, ONE big rusted spiked pauldron, rust bracer + greaves, bone necklace. Outline works; up close "a man in a leather cap with a ghost tunic".
- Witch-Bound Emerald v1 (19:44, not sent): too subtle; v2 killed mid-run on the ruling.

**Dom's verdict (via Strategy, 19:3x):** a re-dress or re-material of today's meshes reads "same same as what we have"; lighting is worth 5–10 % at most; sets are judged against the guide images in `docs/briefs/armour-sets/`. Hero Look's generated Sand Legionary (FLUX → TRELLIS.2 → fit; branch `herolook/sand-legionary`, sources in `docs/character-references/sand-legionary-*`) passed the direction test.

**Ruling (Strategy via Lead, 19:5x) — the split.** Hero Look owns the generation recipe and the hero-fit tooling; ARMOUR owns the SETS built with it. Bronze Hoplite and Scrap-King are approved DESIGNS; Armour re-issues each through the recipe, one set each, after Hero Look's 21:15 fixes (own head, hand, shield seat) prove the fit; Armour operates the pipeline from Hero Look's notes (`docs/state/herolook.md`). Armour keeps: the shield-rim fix (#849, after #843's loot.glb rebuild), the fit/clipping audit, the fight-camera readability bar. Everything goes through Lead.

**Live defect found on the way:** the shared kit shield's iron rim stands EDGE-ON to the disc on every shield wearer (live 36d4aecc; `evidence/armour-sets` folder `live-36d4aecc/`). Cause: the rim torus is born in XY and `lie()` turns it into XZ; fix `.rotateX(Math.PI / 2)` before `lie` — PR #849 (armour/shield-rim-face-plane dd2fc1f8); its loot.glb + carriers + layers rebuild waits for #843 so two rebuilds do not race. Crest on live: seated, no float (the earlier "floats on trunk" line was wrong and was corrected).

**Remaining validation:** #849 rebuild + tests + before/after still after #843 merges; Hero Look's fit fixes at 21:15; then the Hoplite through the recipe.

## Now — armour lane, as of 2026-09-26 20:3x +04 (replace this section wholesale; it is the restart brief, not history)

**Who you are.** You own the hero's rank armour SETS and every non-weapon piece a fighter wears. Since 19:5x (Strategy ruling via Lead) sets are built with Hero Look's GENERATION recipe (FLUX.1-dev → Kontext A-pose → TRELLIS.2 1536 → Blender fit on the hero rig → stills); Hero Look owns the recipe and the fit tooling (`docs/state/herolook.md`, branch `herolook/sand-legionary`), you own the sets. Everything goes through Lead (uds socket in your inbox; name "Frankendom - Lead Developer"). Never send to Strategy directly.

**Rules that stand.** Silhouette first, material second, colour third; each set = one or two BIG readable devices (crest, pauldron mass, shield with a bold painted device, skirt length), not engraving; judged against the guide images in `docs/briefs/armour-sets/` (guides, not specs). Budget for hero sets: up to 80k tris at close-up cameras + an automatic sub-30k LOD for the fight camera, textures 2048 close-up / 1024 fight, phone frame time measured and reported. Heavy local steps (Blender fit ≈6 min, renders) only at Lead's FREE and in slot order (Hero Look → Veteran → you), load < 30 first; remote ZeroGPU steps (t2i, kontext, trellis2) need no FREE. First visible artefact within 30 min of starting any item; reply "ack" or the exact blocker, no re-estimates.

**Bronze Hoplite — where it is (20:3x).** Design approved (Hoplite v2 re-dress passed the fight-camera outline test). Sources on `origin/armour/hoplite-sources` 25f6d769: chosen A-pose image `docs/character-references/bronze-hoplite-source-b3.png` (FLUX candidate b → Kontext warm bronze → Kontext A-pose; receipts beside each PNG). TRELLIS.2 at 1536 done: `src/assets/source/creatures/hoplite.glb` (5.66 MB, untracked; regenerate with `trellis2.py --image …b3.png --name hoplite --resolution 1536` if missing). Probe: crest = top 8 % of the mesh, crown at 92 % → CREATURE_HEIGHT=1.95 puts the crown at 1.79 m (Hero Look's legionary rule); arms ≈23° from vertical in the mesh. Fit tooling for it: `origin/armour/hero-sets-tooling` 539e3489 (off Hero Look's 06e5531e: HERO_SETS generalisation, the `hoplite` row, CREATURE_TRIS budget override) — sent to Hero Look for adoption; check whether Hero Look merged/amended it before using. NEXT, at FREE+10: `CREATURE_OUT=public/herolook/hoplite.glb CREATURE_HEIGHT=1.95 CREATURE_TRIS=80000 node scripts/build-creatures.mjs hoplite` (CREATURE_ARM 62 first; adjust if the fingertip gap print complains), then `node scripts/herolook-stills.mjs --pilot public/herolook/hoplite.glb --label hoplite-v1` + `scripts/herolook-strip.py` → Profile pair to Lead; then kill screen + fight camera; then the round shield with the red device via the recipe's step 6 (product-shot t2i → trellis2 → herolook_scutum.py). If Hero Look's generator bake-off picks another generator, re-run the Hoplite on it.

**Then:** Goblin Scrap-King through the same recipe (approved design: bare chest, horned scrap helm, ONE big spiked pauldron, asymmetry; scratch v3 strip on `evidence/armour-sets` f35285bf). Witch-Bound Emerald not started (re-dress stopped). The fit/clipping audit stays yours.

**Also open.** #849 shield rim face-plane (armour/shield-rim-face-plane dd2fc1f8, draft): after #843's loot.glb rebuild is on trunk, merge trunk, rebuild loot.glb + split-loot + loot-layers from scratch, tests, 375 Profile before/after of the Centurion shield, READY to Lead. #851 = this state doc (docs only). Ten-rung sheets are CANCELLED (tint stays as is). Session worktree: `.claude/worktrees/laughing-meitner-7d47c2` holds untracked scratch (harness `scripts/armour-contact-sheet.mjs` newer than #832, Hero Look's stills scripts copied in, `scripts/character/.t2i-herolook.py`) plus Hero Look's overlaid tracked files (creatures.py, creature_pack.py, build-creatures.mjs, src/scene.ts ?hero= switch) — restore or re-overlay from `herolook/sand-legionary` as needed; the sets scratch patch is in the scratchpad (`sets-scratch.patch`).

**How you work.** Read `AGENTS.md`, `PROJECT_STATE.md`, this file, then `docs/state/herolook.md` RECIPE. Report milestones to Lead. Memory at `~/.claude/projects/-Users-domininclynch-Developer-frankendom-armour/memory/`.

## Done today
- 2026-09-26 17:2x: #831 re-headed d6734541 (trunk 20400d2d merged, CLASS_OF keeps Wrap: 'cloth' and Felt: 'cloth'; gate 703/0) and #836 re-headed bc4a81a7 (gate 704/0): both READY, in Deploy's run. Crest slot built on armour/crest-slot (see Now). Harness gained `--sets`, `--rungs` and the readability numbers (uncommitted in the app worktree at scripts/armour-contact-sheet.mjs; #832 carries the earlier version, push the new one to it).
- 2026-09-26: **#831 Plague Doctor hat** (armour/pd-hat-felt @ 5ffaf355): crown and brim on a new plain `Felt` material (roughness .9, cloth in grades.ts); the family's ORM atlas is 256 px and tops out near roughness .7, so no baked patch could go matte. plaguedoctor out of NOT_WORN. Stills origin/evidence/armour-pd-hat. Gate 677/0.
- 2026-09-26: **#832 contact-sheet harness** (`scripts/armour-contact-sheet.mjs`, `--sets` for whole kits): 64 hero pieces at the fight camera + Profile at 375, trunk 9b07457d; sheets origin/evidence/armour-audit-0926; the 16-item list went to Lead and became the order above.
- 2026-09-26: **loot-layers re-baseline** (this PR): two renders of one loot.glb diff byte-identical, so trunk's 12 differing layers (dwarf.Boots, the Knight's six, the Doctor's five) were stale, rendered before the KnightIron / PlaguedoctorCloth bakes. Re-rendered from trunk 639bf1e1's loot.glb; `loot-layers.mjs` now stamps the loot.glb + warrior.glb hashes into the style.css block and `tests/loot-layers.test.ts` fails when they drift. Before/after: origin/evidence/armour-layers-rebaseline.
- 2026-09-26: lane opened (folder, identity file, this doc).

## Open
- Dom's rulings on the rank sheet (helmet-only ladder? wraps as cloth?).
- Crest branch (Veteran) and `char/rank-tint` (Character Main), via Lead, after Dom judges.
- Crest-slot PR: stills, browser rows, gate, un-draft, READY (after the current run).
- Web: restyle the #slot-crest card (unreachable at 17:0x; details in the crest PR body).

## Gotchas
- witch.Body's Profile layer has two byte-variants across renders (933 px, max channel delta 4, invisible); leave that file untouched in every PR. Everything else is byte-identical across renders.
- The deploy-lock hook refuses a WHOLE command that contains a test/build/browser run, single-file tests included; keep edits and test runs in separate commands while a deploy is in flight.
- A branch based on newer trunk rebased onto a lane branch based on older trunk replays trunk's commits: merge the lane branch in instead.
- `node_modules` is a symlink to the strategy worktree's; run `npm ci` here if a check needs a clean tree.
- loot.glb is rebuilt from per-piece sources; do not hand-edit the built file. Pipeline after any piece change: `WARRIOR_LOOT=1 node scripts/build-warrior.mjs` (needs untracked artifacts/source with animations2: `~/Developer/frankendom-char` has it) → `node scripts/split-loot.mjs` (all ten carriers change bytes) → `node scripts/loot-layers.mjs` (deterministic; the stamp test catches a forgotten run).
